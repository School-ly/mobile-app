android {
            defaultConfig {
                applicationId = "org.nativescript.blankts"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }